# MLForge Project Rules
- Backend: Python + FastAPI + SQLAlchemy + SQLite
- Frontend: Plain HTML/CSS/JS (no frameworks) — I'm learning JS, so keep it simple and well-commented
- ML: scikit-learn (KNN, Decision Tree, Random Forest, Logistic Regression)
- Auth: JWT-based
- Explain WHY each piece of code works, not just what it does — I'm learning
- Don't add extra features I haven't asked for
- Follow this folder structure: backend/routers, backend/ml, backend/storage, frontend/css, frontend/js
